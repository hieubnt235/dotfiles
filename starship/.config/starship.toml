format = '''
[┌─>](bold green) $git_branch$python\[$username$hostname\]
[├─>](bold green) $directory
[└──────────>](bold green) '''


[username]
# style_user = 'red bold'
# style_root = 'black bold'
format = '$user'
disabled = false
show_always = true


[hostname]
ssh_only = true
format = '@[$hostname](bold red)[$ssh_symbol](bold blue)  '
disabled = false

[directory]
truncation_length = 0
truncate_to_repo = false
truncation_symbol = '…/'

[python]
format='[(\($virtualenv ${symbol}${pyenv_prefix}\) )]($style)'
symbol='🐍'

[git_branch]
format='[$symbol$branch(:$remote_branch)]($style) '
